name: Publish to npm

# Publishes @selat-ai/siwx-refund to npm via Trusted Publishing (OIDC).
# No NPM_TOKEN secret required — npm verifies the publish via the
# GitHub OIDC token issued to this workflow. Configure on the npm side
# at https://www.npmjs.com/package/@selat-ai/siwx-refund/access by adding
# this repo + workflow as a trusted publisher.
#
# Triggers:
#   - Pushing a tag like `v0.2.0` runs the full publish.
#   - workflow_dispatch lets you run a dry-run (default) or a full
#     publish (dry-run: false) from the GitHub UI.

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      dry-run:
        description: 'Dry-run only (skip the actual publish)'
        type: boolean
        default: true

permissions:
  id-token: write   # required for OIDC trusted publishing
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
          package-manager-cache: false

      - name: Verify tag matches package.json version
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          PKG_VERSION=$(node -p "require('./package.json').version")
          if [ "$TAG_VERSION" != "$PKG_VERSION" ]; then
            echo "::error::Tag v${TAG_VERSION} does not match package.json version ${PKG_VERSION}"
            exit 1
          fi
          echo "Tag and package.json both at ${PKG_VERSION}"

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

      - name: Dry-run pack
        run: npm pack --dry-run

      - name: Upgrade npm (trusted publishing requires >=11.5.1)
        run: npm install -g npm@latest

      - name: Publish
        if: ${{ !(github.event_name == 'workflow_dispatch' && inputs.dry-run) }}
        run: npm publish --access public
